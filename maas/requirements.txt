# MaaS training + serving deps. Versions pinned so the artifact trained offline unpickles
# under the exact same scikit-learn at serve time — train/serve parity.
# features.py itself is pure stdlib; train.py reads the CSV with the stdlib csv module
# (no pandas needed). FastAPI/uvicorn added in Phase 4 for the /predict service.
scikit-learn==1.5.2
numpy==2.1.3
joblib==1.4.2
fastapi==0.115.6
uvicorn[standard]==0.34.0
