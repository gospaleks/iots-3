# Copy to .env for local dev. In containerized builds this is baked at build-time
# (docker-compose passes it as a build arg). Point the app at the Analytics service,
# which serves REST /api/* and Socket.IO on the same origin.
VITE_API_URL=http://localhost:3003
