{
  "name": "@iots/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared message contract: SensorMessage DTO, topic + env-key constants.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
